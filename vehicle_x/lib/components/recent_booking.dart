import 'package:flutter/material.dart';

class RecentBooking extends StatelessWidget {
  const RecentBooking({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Recent Bookings Component");
  }
}
